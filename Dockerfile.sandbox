FROM alpine:3.18

# Install both C++ and Python
RUN apk add --no-cache \
    g++ \
    musl-dev \
    python3 \
    py3-pip \
    && ln -sf python3 /usr/bin/python

# Create a non-root user for security
RUN adduser -D -u 1000 sandbox

WORKDIR /workspace
USER sandbox
